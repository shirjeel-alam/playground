<div class='container'>
  <div class='row'>
    <div class="page-header"><h1>Photos</h1></div>
    <%= link_to 'Create Photo', new_photo_path, class: 'btn btn-primary pull-right' %>
    <%= render @photos %>
  </div>
</div>